#include "main.h"
#include <string.h>
#include <stdio.h>
/**
 *_strlen_recursion - returns the value the string.
 *@s: The string
 *Return: The value of string.
 */
int _strlen_recursion(char *s)
{
	int i = strlen(s);

	return (i);
}
